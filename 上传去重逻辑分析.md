# 上传去重逻辑场景分析

## 核心代码位置

| 组件 | 代码位置 |
|------|----------|
| `getDeviceAssetId` | [file-uploader.ts#L126-L128](file:///c:/Users/10244/Desktop/0508-under/immich/web/src/lib/utils/file-uploader.ts#L126-L128) |
| `uploadAssetsStore.addItem` | [upload.ts#L24-L46](file:///c:/Users/10244/Desktop/0508-under/immich/web/src/lib/stores/upload.ts#L24-L46) |
| `uploadExecutionQueue` | [executor-queue.ts](file:///c:/Users/10244/Desktop/0508-under/immich/web/src/lib/utils/executor-queue.ts) |
| 前端 hash 检查 | [file-uploader.ts#L130-L211](file:///c:/Users/10244/Desktop/0508-under/immich/web/src/lib/utils/file-uploader.ts#L130-L211) |
| `checkBulkUpload` 后端 | [asset-media.service.ts#L252-L280](file:///c:/Users/10244/Desktop/0508-under/immich/server/src/services/asset-media.service.ts#L252-L280) |
| `AssetUploadInterceptor` | [asset-upload.interceptor.ts](file:///c:/Users/10244/Desktop/0508-under/immich/server/src/middleware/asset-upload.interceptor.ts) |
| 后端 multipart 落盘 | [file-upload.interceptor.ts#L97-L137](file:///c:/Users/10244/Desktop/0508-under/immich/server/src/middleware/file-upload.interceptor.ts#L97-L137) |
| 数据库 checksum 约束 | [asset.table.ts#L32-L42](file:///c:/Users/10244/Desktop/0508-under/immich/server/src/schema/tables/asset.table.ts#L32-L42) |
| 相册添加 | [file-uploader.ts#L236-L240](file:///c:/Users/10244/Desktop/0508-under/immich/web/src/lib/utils/file-uploader.ts#L236-L240) |
| 共享链接添加 | [asset-media.service.ts#L151-L153](file:///c:/Users/10244/Desktop/0508-under/immich/server/src/services/asset-media.service.ts#L151-L153) |

---

## 三层去重机制说明

### 第一层：前端 `checkBulkUpload` 预检
- 前端计算文件 SHA1 hash
- 调用 `/assets/bulk-upload-check` 接口检查数据库中是否存在相同 checksum
- 如果存在，直接标记为重复，不上传文件

### 第二层：`AssetUploadInterceptor` 请求头检查
- 读取 `x-immich-checksum` 请求头
- 如果请求头存在且数据库中有匹配，直接返回 200 和重复状态
- **注意**：当前 Web 前端的 `uploadRequest` 函数 [utils.ts#L99-L128](file:///c:/Users/10244/Desktop/0508-under/immich/web/src/lib/utils.ts#L99-L128) 并未设置此请求头，因此第二层检查对 Web 上传实际上不生效

### 第三层：数据库唯一约束
- 数据库表 `asset` 有唯一约束 `UQ_assets_owner_checksum` (ownerId + checksum)
- 插入时如果违反约束，在 `handleUploadError` [asset-media.service.ts#L288-L318](file:///c:/Users/10244/Desktop/0508-under/immich/server/src/services/asset-media.service.ts#L288-L318) 中捕获并处理为重复

---

## 场景一：文件名和 lastModified 相同，二进制内容不同

**测试用例：**
- 文件 A：`name=photo.jpg`, `lastModified=1717000000000`, 内容 = "AAA" → checksum = `sha1("AAA")`
- 文件 B：`name=photo.jpg`, `lastModified=1717000000000`, 内容 = "BBB" → checksum = `sha1("BBB")`

### 各环节行为分析

| 环节 | 文件 A 行为 | 文件 B 行为 | 说明 |
|------|------------|------------|------|
| **`uploadAssetsStore.addItem`** | ✅ 添加成功，id = `web-photo.jpg-1717000000000`，state = PENDING | ⚠️ **替换已有项**，id 相同，覆盖文件 A 的引用 | 因为 `getDeviceAssetId` 只依赖 name + lastModified，两个文件生成相同的 id，后者覆盖前者的 store 条目。`stats.total` 只增加 1 次 |
| **`uploadExecutionQueue`** | ✅ 添加任务1到队列 | ✅ 添加任务2到队列 | 两个独立任务，并发执行（concurrency=2） |
| **前端 hash 检查** | ✅ 计算 hash = `sha1("AAA")`，调用 `checkBulkUpload`，无重复，继续上传 | ✅ 计算 hash = `sha1("BBB")`，调用 `checkBulkUpload`，无重复，继续上传 | 两个文件 hash 不同，都通过预检 |
| **后端 multipart 落盘** | ✅ 流式写入磁盘，计算 checksum = `sha1("AAA")`，落盘成功 | ✅ 流式写入磁盘，计算 checksum = `sha1("BBB")`，落盘成功 | 文件内容不同，checksum 不同，都成功落盘 |
| **HTTP 状态码** | ✅ 201 Created | ✅ 201 Created | 都创建新资产 |
| **相册添加** | ❌ **不会执行** | ✅ 如果有 albumId，调用 `addAssetsToAlbums` 添加文件 B 的 assetId | 文件 A 的任务完成时，store 中的条目已被文件 B 覆盖，`updateItem` 更新的是同一个 id，最终只有文件 B 的 assetId 有效。文件 A 的 `addAssetsToAlbums` 使用的是自己的 assetId，但后续 store 更新会被覆盖 |
| **共享链接添加** | ✅ 后端自动添加到共享链接 | ✅ 后端自动添加到共享链接 | 这是在后端 `uploadAsset` 中执行的，每个上传独立处理，两个资产都会被添加 |

### 场景一关键问题

1. **Store 数据丢失**：文件 A 的 store 条目被文件 B 覆盖，导致文件 A 的上传进度和最终状态无法正确跟踪
2. **相册添加不一致**：如果从前端触发相册添加，只有文件 B 会被添加到相册（因为 store 中只有文件 B 的 assetId）
3. **资源浪费**：两个文件都完整上传了，但文件 A 的前端状态被覆盖

---

## 场景二：二进制内容相同，文件名不同

**测试用例：**
- 文件 A：`name=photo1.jpg`, `lastModified=1717000000000`, 内容 = "AAA" → checksum = `sha1("AAA")`
- 文件 B：`name=photo2.jpg`, `lastModified=1718000000000`, 内容 = "AAA" → checksum = `sha1("AAA")`

### 各环节行为分析

| 环节 | 文件 A 行为 | 文件 B 行为 | 说明 |
|------|------------|------------|------|
| **`uploadAssetsStore.addItem`** | ✅ 添加成功，id = `web-photo1.jpg-1717000000000` | ✅ 添加成功，id = `web-photo2.jpg-1718000000000` | 两个不同的 id，都成功添加，`stats.total` 增加 2 |
| **`uploadExecutionQueue`** | ✅ 添加任务1到队列 | ✅ 添加任务2到队列 | 两个独立任务，并发执行 |
| **前端 hash 检查** | ✅ 计算 hash = `sha1("AAA")`，调用 `checkBulkUpload`，无重复，继续上传 | ✅ 计算 hash = `sha1("AAA")`，调用 `checkBulkUpload`，**结果取决于执行时序** | 如果文件 A 还未写入数据库，文件 B 的 `checkBulkUpload` 也会返回无重复；如果文件 A 已写入，文件 B 会被标记为重复，跳过上传 |
| **后端 multipart 落盘** | ✅ 流式写入磁盘，checksum = `sha1("AAA")` | ✅ 如果通过了前端检查，也会落盘 | 两个文件可能都会落盘（取决于时序），但只有第一个能成功写入数据库 |
| **数据库约束检查** | ✅ 插入成功，生成 assetId-A | ❌ 插入失败，违反 `UQ_assets_owner_checksum` 约束 | 第二个文件会触发数据库唯一约束错误，被 `handleUploadError` 捕获 |
| **HTTP 状态码** | ✅ 201 Created | ✅ 200 OK（重复） | 第二个文件返回 200 和重复状态，携带文件 A 的 assetId |
| **相册添加** | ✅ 添加 assetId-A 到相册 | ✅ 也添加 assetId-A 到相册（重复添加，后端会去重） | 两个文件都会触发相册添加，但添加的是同一个 assetId，相册中只会出现一次 |
| **共享链接添加** | ✅ 添加 assetId-A 到共享链接 | ✅ 在 `handleUploadError` 中也会添加 assetId-A 到共享链接 | 重复添加，后端会去重 |

### 场景二时序分析

#### 时序 A：文件 A 先完成数据库写入，文件 B 才开始 `checkBulkUpload`
```
文件A: checkBulkUpload → 无重复 → 上传 → 落盘 → 写入DB (201)
文件B: checkBulkUpload → 发现重复 (assetId-A) → 标记为 DUPLICATED → 不上传
```
- 结果：只有文件 A 上传，文件 B 在前端就被拦截，节省带宽

#### 时序 B：文件 B 的 `checkBulkUpload` 在文件 A 写入 DB 前执行
```
文件A: checkBulkUpload → 无重复 → 上传 → 落盘 → 写入DB (201)
文件B: checkBulkUpload → 无重复 → 上传 → 落盘 → 写入DB失败 (约束错误) → 返回200重复
```
- 结果：两个文件都上传了，但文件 B 浪费了带宽，最终在数据库层被拦截

---

## 共享链接上传的特殊说明

当通过共享链接上传时（`authManager.isSharedLink = true`）：

1. **前端 hash 检查被跳过**：[file-uploader.ts#L192](file:///c:/Users/10244/Desktop/0508-under/immich/web/src/lib/utils/file-uploader.ts#L192) 的条件判断 `!authManager.isSharedLink` 导致跳过前端 hash 检查
2. **相册添加被跳过**：同样在 [file-uploader.ts#L236](file:///c:/Users/10244/Desktop/0508-under/immich/web/src/lib/utils/file-uploader.ts#L236) 被跳过
3. **共享链接添加由后端处理**：在 [asset-media.service.ts#L151-L153](file:///c:/Users/10244/Desktop/0508-under/immich/server/src/services/asset-media.service.ts#L151-L153) 和 [asset-media.service.ts#L308-L310](file:///c:/Users/10244/Desktop/0508-under/immich/server/src/services/asset-media.service.ts#L308-L310)（重复场景）中处理

---

## 总结

| 场景 | 主要问题 | 影响 |
|------|----------|------|
| **同名+同时间，不同内容** | `getDeviceAssetId` 冲突导致 store 条目覆盖 | 前端状态跟踪混乱，相册添加可能遗漏 |
| **同内容，不同名** | 并发上传时可能重复上传，浪费带宽 | 带宽浪费，但数据库层最终保证唯一性 |

### 潜在改进点

1. **`getDeviceAssetId` 应包含内容哈希**：避免仅靠文件名和时间戳导致的 ID 冲突
2. **前端应设置 `x-immich-checksum` 请求头**：激活第二层去重检查，在文件上传前就拦截重复
3. **并发上传时序问题**：可以考虑在前端对相同 checksum 的上传进行排队，避免并发冲突
